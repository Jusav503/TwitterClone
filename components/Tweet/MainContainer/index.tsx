import React from "react";
import { View, Text } from "react-native";

export type MainContainerProps = {};
const MainContainer = (props: MainContainerProps) => {
  return (
    <View>
      <Text>This is a maincontainer</Text>
    </View>
  );
};

export default MainContainer;
